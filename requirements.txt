# 凭证管理系统依赖包

# 核心依赖
cryptography>=41.0.0      # 加密存储
pyyaml>=6.0              # YAML文件解析
python-dotenv>=1.0.0     # 环境变量管理
aiohttp>=3.9.0           # 异步HTTP客户端
uvloop>=0.19.0          # 高性能事件循环

# 可选依赖（用于高级功能）
matplotlib>=3.7.0        # 监控图表（可选）
aiofiles>=23.0.0        # 异步文件操作（可选）
rich>=13.0.0            # 美化控制台输出（可选）

# 开发依赖
pytest>=7.4.0           # 测试框架
pytest-asyncio>=0.21.0  # 异步测试支持
black>=23.0.0           # 代码格式化
flake8>=6.0.0           # 代码检查
mypy>=1.5.0             # 类型检查